# Personal preferences files — never commit real user preferences.
# These paths are how personalization stays out of this public repo.
.claude/preferences.yaml
.claude/preferences.yml
skills-preferences.yaml
skills-preferences.yml
**/skills-preferences.yaml
**/skills-preferences.yml

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Python (tooling scripts, Phase 4+)
__pycache__/
*.pyc
.venv/
venv/

# Note: dist/*.skill packaged bundles ARE committed intentionally, so
# downloading a single skill doesn't require cloning the whole repo or
# running tools/package_skill.py locally. Regenerate them with:
#   for s in skills/*/; do python3 tools/package_skill.py "$(basename "$s")"; done

# Local scratch
.tmp/
*.local
